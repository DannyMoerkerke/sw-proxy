module.exports = MiddlewareBase => class Hangs extends MiddlewareBase {
  middleware () {
    return async function (ctx, next) {
      const request = require('req-then')
      console.log('Req')
      const response = await request('http://www.bbc.co.uk')
      console.error(require('util').inspect(response, { depth: 6, colors: true }))
    }
  }
}
