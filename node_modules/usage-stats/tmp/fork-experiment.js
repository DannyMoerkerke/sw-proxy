const cp = require('child_process')

const child = cp.spawn('node', [ 'spam-files.js' ], {
  detached: true,
  stdio: 'ignore'
})

child.unref()
console.error(require('util').inspect(child.pid, { depth: 6, colors: true }))
